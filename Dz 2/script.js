const First = parseInt(prompt('Введите первое число'))

const Second  = parseInt(prompt('Введите второе число'));

// if(First=isNaN, Second=isNaN 
//     {console.log("Ошибка вы ввели не число")}
// )


for (let i = First; i < Second; i++) {
        const element = i;
        console.log(i)
        
    }
